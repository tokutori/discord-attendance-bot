# AGENTS.md

このファイルは、このリポジトリを扱うAIコーディングエージェント向けの指示である。

## 秘密情報の取り扱い

- `.env`、`.env.*`、API key、Bot token、パスワード、Cookie、秘密鍵、接続文字列などの秘密情報を取得・閲覧・検索・表示・コピー・復号・推測してはならない。
- API key等の秘密情報を取得するための試行を行ってはならない。秘密情報を取得しようとしてはならない。
- 環境変数を一覧表示したり、秘密情報を含む可能性のあるファイルをダンプしたりしてはならない。
- 設定項目の確認が必要な場合は、値ではなく `.env.example`、変数名、型、存在有無だけを扱うこと。
- 秘密情報が出力、ログ、テスト結果、パッチ、コミットに現れた可能性がある場合は、内容を再表示せず作業を止めて報告すること。
- `.env` をGitへ追加するための `git add -f` などの除外回避操作を行ってはならない。
- Botの実行時にアプリケーションが環境変数を読むことはあるが、エージェントが秘密情報を読むことを許可するものではない。秘密情報を必要とする実行や外部サービス接続は、ユーザーが明示的に依頼した場合だけ行うこと。

## 開発ルール

- テキストファイルはUTF-8で保存すること。PowerShellで読み書きする場合はUTF-8を明示すること。
- 変更後は `cargo fmt --check`、`cargo test`、`cargo clippy --all-targets --all-features -- -D warnings` を実行すること。
- `cargo run` はDiscord tokenを使って外部接続する可能性があるため、ユーザーの明示的な依頼なしに実行しないこと。
- 秘密情報を使わない単体テストを優先し、テスト用の値はダミー値にすること。
